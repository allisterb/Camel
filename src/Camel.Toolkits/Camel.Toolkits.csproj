<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
  </ItemGroup>

  <!-- The DFIR toolkit logic classes moved to Camel.DFIR.Toolkits but still use internal model
       helpers (e.g. WebCacheParse) that were same-assembly before the split. Restore that access. -->
  <ItemGroup>
    <InternalsVisibleTo Include="Camel.DFIR.Toolkits" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Camel.Environments\Camel.Environments.csproj" />
    <ProjectReference Include="..\Camel.Runtime\Camel.Runtime.csproj" />
  </ItemGroup>

</Project>
